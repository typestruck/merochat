{
  "name": "melanchat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "repl-s": "pulp repl --src-path src/Server --include src/Shared/",
    "watch-s": "pulp --watch run -o output/Server --src-path src/Server --include src/Shared/",

    "repl-l": "pulp repl --src-path src/Client/Landing --include src/Shared:src/Client/Common:src/Client/External",

    "w-landing": "pulp --watch browserify -o output/Client/landing --src-path src/Client/Landing --main Client.Landing.Main --include src/Shared:src/Client/Common:src/Client/External --to src/Client/javascript/landing.bundle.js --source-maps --standalone Landing",

    "watch-c": "npm run w-landing",

    "watch": "npm run watch-s & npm run watch-c",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
  }
}
